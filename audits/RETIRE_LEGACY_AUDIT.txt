RETIRE LEGACY SCHEDULER — SELF-AUDIT
=====================================
Branch:  retire-legacy-scheduler
Base:    0e5a0da (phase H: feedback loop)
Date:    2026-09-09
Goal:    Delete the legacy AutomationRule scheduler entirely. The Brain (Phase C)
         is now the only thing that schedules ScheduledJob rows.


1. FILES DELETED
----------------
  backend/utils/automationEngine.js          (137 lines) — scheduleCartAbandonJobs, cancelJobsForOrder
  backend/models/AutomationRule.js           ( 30 lines) — the rule schema
  backend/routes/automation.js               (159 lines) — AutomationRule CRUD router (mounted at /api/automation)
  backend/__tests__/automationEngine.test.js (283 lines) — 15 tests for the deleted engine

  Confirmed absent on disk (test -f → not found for all four).
  git status shows: D backend/utils/automationEngine.js
                    D backend/models/AutomationRule.js
                    D backend/routes/automation.js
                    D backend/__tests__/automationEngine.test.js


2. FILE CREATED
---------------
  backend/scripts/retireLegacyJobs.js — one-shot migration.
    Connects with dns.setServers(['8.8.8.8','1.1.1.1']) (SRV DNS workaround on this box).
    - ScheduledJob.updateMany({ status:'pending', ruleId:{$type:'objectId'} },
                              { status:'cancelled', reason:'legacy_retired' })
    - Then counts remaining pending brain jobs (ruleId:null, profileId set).
    Note: spec's require('./models/ScheduledJob') corrected to '../models/ScheduledJob'
    because the script lives in backend/scripts/.


3. IMPORTS / CALLS REMOVED
--------------------------
  backend/server.js
    - line 16  removed:  const AutomationRule = require('./models/AutomationRule');
    - lines 132-133 removed:  const automationRouter = require('./routes/automation');
                              app.use('/api/automation', automationRouter);
    - 2 comments in processScheduledJobs reworded (no longer name AutomationRule):
        "Legacy AutomationRule jobs keep their merchant-authored payload."
      → "Legacy rule-scheduled jobs (profileId unset) keep their frozen payload."
      The `if (job.profileId)` guard around generateCopy is UNCHANGED — historical
      rule-scheduled rows (profileId null) still send their frozen payload untouched.

  backend/routes/webhooks.js
    - line 7 removed:
        const { scheduleCartAbandonJobs, cancelJobsForOrder } = require('../utils/automationEngine');
    - handleWebhook: the `if (source === 'cart' && doc.sessionId) { ... scheduleCartAbandonJobs(...) }`
      block removed, replaced with a comment (webhooks.js:237-240). Cart webhooks now only
      update the AbandonedCustomer row + fetch the product image; no timer is armed.
    - handleOrderWebhook: `await cancelJobsForOrder(shopDomain, cartToken, customerId)` replaced
      with an inline ScheduledJob.updateMany cancel-on-order block (webhooks.js:272-286).
      DEVIATION FROM SPEC: spec said just remove the call. Kept the behaviour inline so a
      shopper who converts stops getting pending reminders (works on brain jobs too, since it
      matches on cartToken/customerId, not ruleId). ScheduledJob was already a top-level
      require in webhooks.js (added in Phase C2).
    - handleShopRedact: removed `const AutomationRule = require('../models/AutomationRule')`,
      removed `AutomationRule.deleteMany({ shopDomain })` from the Promise.all([...]), and
      dropped a now-redundant inline `const ScheduledJob = require(...)` (top-level require covers it).

  backend/models/ScheduledJob.js
    - `ruleId` field: removed `ref: 'AutomationRule'` (dangling ref to a deleted model).
      Field KEPT (plain ObjectId, default null) — historical rows still carry it and the
      "is this a brain job?" check (`ruleId == null`) and the two partial indexes
      (partialFilterExpression ruleId:{$type:'objectId'}) still work as-is. Harmless dead
      field name; not worth a data migration.

  backend/models/SignalConfig.js
    - header comment reworded to drop "Replaces AutomationRule" → "replaced the old rule editor".


4. MIGRATION RUN
----------------
  $ node scripts/retireLegacyJobs.js
    [retire] Cancelled 0 legacy pending jobs
    [retire] Brain jobs still pending: 5

  0 legacy pending jobs existed (the 21 legacy rows seen in the V2 completion audit were
  already sent/expired/cancelled). 5 brain-scheduled jobs are pending — the Phase C2
  on-ingest signal pipeline is producing, and those are unaffected.


5. TESTS
--------
  $ cd backend && npm test
    Test Suites: 13 passed, 13 total     (was 14 — automationEngine.test.js suite removed)
    Tests:       116 passed, 116 total    (was 131 — 15 automationEngine tests removed)
    Time:        ~7 s

  No other suite referenced automationEngine or AutomationRule, so nothing else changed.

  $ node --check server.js          → OK
  $ node --check routes/webhooks.js → OK
  (also node --check on models/ScheduledJob.js, models/SignalConfig.js, scripts/retireLegacyJobs.js — all OK)


6. REMAINING REFERENCES
-----------------------
  grep -rn 'AutomationRule|automationEngine|scheduleCartAbandonJobs|cancelJobsForOrder' backend/
  (excluding node_modules) returns 4 lines, ALL retirement-explaining comments, ZERO code:

    backend/scripts/retireLegacyJobs.js:11   // ...jobs that have a ruleId (legacy AutomationRule jobs).
    backend/routes/webhooks.js:240           //  legacy AutomationRule scheduler was retired.)
    backend/routes/webhooks.js:274           // ...automationEngine.cancelJobsForOrder was retired...
    backend/models/ScheduledJob.js:5         // Legacy field. The AutomationRule model was retired...

  No require(), no route mount, no function call. The /api/automation endpoint is gone.


7. DIFF STAT (retire-legacy files only)
---------------------------------------
  backend/__tests__/automationEngine.test.js | 283 --------
  backend/models/AutomationRule.js           |  30 ---
  backend/models/ScheduledJob.js             |   5 +-
  backend/routes/automation.js               | 159 --------
  backend/routes/webhooks.js                 |  43 ++--
  backend/server.js                          |   7 +-
  backend/utils/automationEngine.js          | 137 --------
  (+ backend/scripts/retireLegacyJobs.js, untracked, new)

  (push-notifications.liquid also shows in git status — that is the earlier dwell-timeout
   tweak, not part of this phase.)


8. DEVIATIONS
-------------
  (a) cancelJobsForOrder removed as an import but its behaviour re-implemented inline in
      handleOrderWebhook (10 lines, plain ScheduledJob.updateMany). Rationale: without it,
      a shopper who places an order keeps receiving pending brain reminders — a regression.
  (b) ScheduledJob.ruleId field retained (only `ref:` dropped). No historical-data migration;
      the field is inert but read by the brain-job discriminator and two partial indexes.
  (c) Migration script path fixed from spec ('./models' → '../models').


STATUS: COMPLETE. Legacy AutomationRule scheduler fully retired. 13 suites / 116 tests green.
Stopping here — no further phase started.
