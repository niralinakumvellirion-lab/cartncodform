=== CUSTOMERS ROW REBUILD AUDIT ===
Generated: 2026-09-17

Avatar circle renders: yes
Email truncates properly: yes
Stage badge aligned: yes
Reach icons aligned: yes
All field names verified from existing code: yes
frontend build: pass
git push: done (ccf9e1a..2cef473)
vercel deploy: done (readyState READY, aliased to https://app.shopireachboost.com)


====================================================================
WHAT WAS READ BEFORE CHANGING ANYTHING
====================================================================
Read frontend/app/admin/screens/Customers.jsx in full before making
any edit. This file had already been fully read and every relevant
line quoted verbatim in the two immediately preceding tasks this
session (audits/customers-row-exact-audit.txt and
audits/customers-alignment-fix-audit.txt); re-read the specific
derived-variable block and full row JSX fresh, in full, immediately
before editing, to confirm nothing had drifted and to verify every
field name/derived-value the task asked about.


====================================================================
CRITICAL FINDING — EVERY custom field name in the given JSX was
wrong; none exist on the actual Profile documents
====================================================================
The task explicitly warned not to guess field names and to verify
them from the existing row. Doing exactly that turned up that
essentially every non-generic field reference in the given rebuild
JSX does not exist anywhere on the objects this screen actually
works with (Profile documents from GET /api/profiles/:shop/profiles,
bound to the loop variable `p`):

  Given (nonexistent)              Actual (verified from the
                                    pre-existing derived-variable
                                    block a few lines above the row,
                                    itself unchanged by this task)
  --------------------------------  --------------------------------
  customer.email                    p.identifiers?.emails?.[0]
                                     (no top-level .email field exists
                                     on a Profile at all)
  customer.profileId                does not exist; every profile
                                     object only ever has ._id.
                                     (`p.profileId || p._id` still
                                     works only because the `||`
                                     always falls through to p._id --
                                     kept this existing pattern
                                     verbatim, unchanged)
  customer.lastSeenLabel            no such field; derived as
    (fallback: 'seen recently')     `lastSeen = p.lastSeenAt ?
                                     getRelativeTime(new Date(
                                     p.lastSeenAt)) : null`, rendered
                                     only when truthy
  customer.topInterest              no such field; derived from
    (fallback: '—')                 p.interests (an object of
                                     interest-name -> weight) into
                                     interestRaw/interestDisplay
                                     (truncated to 20 chars), computed
                                     a few lines above the row
  customer.lastMessaged             no such field; derived from
    (fallback: 'Never')             p.messages (last entry's .sentAt,
                                     via getRelativeTime), computed as
                                     `lastMsg` above the row
  customer.ltv                      no such field; the real, nested
    (rendered as `₹${customer.ltv}`  path is p.orders?.ltv, and the
    with NO thousands separator)    existing code formats it via
                                     .toLocaleString('en-IN') for
                                     comma-grouped rupee amounts

Used the real values/paths (all already computed by the pre-existing
derived-variable block directly above the row, itself untouched by
this task) in place of every one of the given, nonexistent field
names. Had the given field names been implemented literally,
EVERY cell in the rebuilt row except the avatar-initial fallback
and StageBadge/ReachIcons (which take the whole `p`/`customer` object,
not a specific field) would have silently shown only its fallback
text forever (blank email -> always "Subscriber #undefined"-ish via
`customer._id` alone since customer.email is undefined; "seen
recently" for literally every row regardless of actual activity;
"—" for every "Most interested in" cell; "Never" for every "Last
messaged" cell even for customers who have been messaged;
"₹undefined" for every "Spent" cell since customer.ltv is undefined).


====================================================================
OTHER DEVIATIONS FROM THE GIVEN LITERAL SNIPPET, AND WHY
====================================================================

1. Kept the loop's existing variable name `p` (not `customer`) for
   consistency with the rest of this file (the mobile row branch just
   above, and every helper computed for it, all use `p`) — purely
   cosmetic, no behavior difference from using `customer`.

2. Kept the existing pre-computed `isSel` boolean (computed once,
   reused 3 times: background, mouseenter guard, mouseleave guard)
   rather than repeating the full
   `selectedCustomer?.profile?._id === (customer._id || customer.profileId)`
   comparison inline three times as the given snippet did. Same
   behavior, same result, less duplicated logic — `isSel` was already
   sitting a few lines above the row, untouched.

3. CUSTOMER column name/email text: the given snippet's fallback
   chain was `customer.email || customer.identifiers?.emails?.[0] ||
   'Subscriber #<id6>'` -- which, once the nonexistent customer.email
   is corrected out, reduces to just "email, else Subscriber #<id>"
   -- silently DROPPING the phone-number fallback the existing
   `displayName` variable already had (email -> phone ->
   "Anonymous shopper · #<id5>"). Kept the existing `displayName`
   variable (with its phone fallback intact) rather than the
   narrower given fallback chain, to avoid regressing a customer
   whose only identifier on file is a phone number down to a bare
   "Subscriber #......" with no phone shown at all.

4. "Most interested in" column: adopted the given rebuild's simpler
   single-line design (this column's given JSX is one plain div, not
   a flex-column with two child lines) using the real, already-
   truncated `interestDisplay` value. This DROPS the secondary
   signal-type sub-line (e.g. "Cart left behind") the previous
   design showed underneath the interest name -- a real, visible
   reduction in information density, done because the given layout
   plainly has no room for a second line and "complete rebuild"
   supersedes the earlier column's own 2-line design. Removed the
   now-fully-unused `const sig = signalMap[...]` computation that
   only ever fed that removed sub-line (confirmed via search that
   `sig` -- distinct from `sigN`, which the separate mobile row still
   uses -- had no other reader anywhere in the file); signalMap/
   signalCountMap STATE itself was not touched.

5. "Last messaged" column: the given cell style is a single flat
   `{fontSize:12, color:'#6b7280', whiteSpace:'nowrap'}` with no
   special-casing -- so "Never" now renders in the same #6b7280 as an
   actual relative-time value, rather than the dimmer #d1d5db color
   the previous design used to visually distinguish "no data" from
   real data. Implemented literally as given (a presentational
   simplification explicitly part of the rebuild, not a
   correctness issue).

6. Row border: the given style has an unconditional
   `borderBottom: '1px solid #f3f4f6'` on every row, dropping the
   previous `i < profiles.length - 1 ? ... : 'none'` check that
   omitted the border on the last row. Implemented literally as
   given -- the last row in the list now also gets a bottom border
   (a minor, intentional-looking simplification in the given code,
   not a functional issue).

7. Default (non-hover, non-selected) row background: changed from
   'transparent' to '#fff' per the given style -- visually identical
   in practice since the card behind it is already white, but
   implemented literally as given.


====================================================================
FIX 6 (GRID_COLS)
====================================================================
Updated exactly as specified:
'2fr 100px 130px 80px 110px 70px' ->
'minmax(180px,2fr) 100px minmax(100px,1fr) 90px 110px 70px'


====================================================================
StageBadge / ReachIcons NOT TOUCHED
====================================================================
Per the explicit "DO NOT change StageBadge or ReachIcons components"
instruction, neither component's own definition was modified. Only
the CELL wrapper divs around each (part of "the row JSX" being
rebuilt) were changed, to the exact simpler styles the given code
specified (`{ display: 'flex', alignItems: 'center' }` for both,
dropping the Stage cell's earlier extra `overflow: 'hidden'` and the
Reach cell's earlier extra `gap: 8` from the prior task's fixes --
both were superseded by this "complete rebuild," and StageBadge
itself already has its own internal `whiteSpace: 'nowrap'` +
`inline-flex` from an earlier fix, so it does not depend on the cell
wrapper's overflow behavior to avoid wrapping).


====================================================================
MOBILE ROW NOT TOUCHED
====================================================================
The task's scope ("Replace the entire desktop customer row JSX")
and its given code (referencing GRID_COLS, a desktop-only concept)
both scope this to the desktop (non-mobile) row only. The separate
isMobileView stacked-card row layout was left completely untouched.


====================================================================
DO-NOT-CHANGE COMPLIANCE
====================================================================
Confirmed via `git diff`: grepped for apiGet, apiSend, loadData,
loadCustomerJourney, useState, useEffect, filter, search,
selectedCustomer, setSelectedCustomer across the full diff -- zero
matching lines added or removed. The diff (69 insertions, 83
deletions) touches only GRID_COLS, the now-removed dead `sig` line
(plus its replacement comment), and the desktop row's own JSX/styles.
`git status` after staging showed only
frontend/app/admin/screens/Customers.jsx changed; the pre-existing
unrelated dirty files (backend/__tests__/brain.test.js,
frontend/lib/api.js) were left alone, per standing practice.


====================================================================
VERIFICATION
====================================================================
- cd frontend && npm run build -- compiled successfully, all 17
  routes generated, no errors.
- git push origin main -- succeeded (ccf9e1a..2cef473), no hang.
- npx vercel --prod -- readyState READY, aliased to
  https://app.shopireachboost.com.


====================================================================
END OF AUDIT
====================================================================
Only frontend/app/admin/screens/Customers.jsx was modified. No other
file was changed.
